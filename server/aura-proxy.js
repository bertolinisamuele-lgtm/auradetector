/* ============================================================================
 * DEPRECATED — superseded by server/index.js + server/ai.js
 * ----------------------------------------------------------------------------
 * This early reference proxy called the AI provider directly. The production
 * implementation now lives in:
 *
 *   server/index.js   secure API, upload validation, rate limiting, admin auth
 *   server/ai.js      AI vision integration (OpenAI / Gemini / OpenRouter)
 *
 * Start the app with:   npm install && npm start
 * Environment setup:     see .env.example
 * Tests:                 npm test
 * ==========================================================================*/
